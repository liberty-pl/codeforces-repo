# Codeforces 跨平台竞赛工作流指南

本文档详细介绍了本工作区的自动化工作流，旨在实现 macOS 和 Windows 之间的无缝切换与同步。

## 📂 目录结构

- **`Current/`**: 核心工作区。存放当前比赛的代码 (`.cpp`) 和测试样例配置 (`.cph/`)。
- **`Archive/`**: 归档区。比赛结束后，代码会被自动归档到这里（按比赛 ID 分类）。
- **`Templates/`**: 存放代码模板（如 `template.cpp`）。
- **`scripts/`**: 存放辅助脚本。
- **`manager.py`**: 核心自动化脚本，负责同步、修复路径、归档和清理。

---

## 🚀 比赛流程

### 1. 赛前准备 (Clean)
在开始新的一场比赛前，确保 `Current` 目录是干净的。

- **操作**: 按 `Cmd+Shift+P` (Mac) / `Ctrl+Shift+P` (Win) -> 输入 `Run Task` -> 选择 **`Codeforces: Clean`**。
- **效果**: 清空 `Current` 目录下的所有代码文件和测试样例，为新比赛腾出空间。

### 2. 抓取题目 (Parse)
使用浏览器插件配合 VS Code 插件自动抓取题目。

- **工具**: 浏览器插件 **Competitive Companion** + VS Code 插件 **CPH**。
- **操作**: 在浏览器题目页面点击绿色加号图标。
- **效果**: 
    - 题目代码会自动生成在 `Current/` 目录下。
    - 自动应用 `Templates/template.cpp` 模板。
    - 测试样例会自动下载并配置好。

### 3. 编写与测试 (Code & Test)
- **编写**: 在 `Current/` 目录下编写代码。
- **测试**: CPH 插件会自动检测代码变化并运行测试样例。

### 4. 提交与同步 (Submit & Sync)
在比赛过程中，建议频繁同步代码，以防意外丢失，并实现跨设备备份。

- **操作**: 按 **`Cmd+Shift+B`** (Mac) / **`Ctrl+Shift+B`** (Win)。
    - 这是默认的 Build 任务，对应 **`Codeforces: Sync`**。
- **效果**: 
    1. `git add` & `git commit`
    2. `git pull` (拉取远程变更)
    3. **自动修复路径** (关键步骤：将 Windows/Mac 的绝对路径差异自动修正，并重命名配置文件哈希，确保 CPH 插件在另一台电脑上也能识别样例)
    4. `git push`

### 5. 赛后归档 (Archive)
比赛完全结束后，将代码归档保存。

- **操作**: 按 `Cmd+Shift+P` -> `Run Task` -> 选择 **`Codeforces: Archive`**。
- **输入**: 在弹出的输入框中输入比赛 ID（例如 `1950`）。
- **效果**:
    - 将 `Current/` 下的所有代码移动到 `Archive/1950/`。
    - 清理 `Current/` 目录。
    - 自动执行一次 Git 同步。

---

## 🛠️ 故障排除

### 跨平台同步后，测试样例不见了？
如果你在 Windows 上抓取了题目，同步到 Mac 后发现 CPH 面板里没有测试样例：
1. 运行任务 **`Codeforces: Fix Paths`** (或者直接运行 Sync 任务)。
2. **重启 VS Code 窗口** (`Cmd+Shift+P` -> `Reload Window`)。
   - *原因：CPH 插件缓存了旧的配置文件名，必须重启才能加载修正后的配置。*

### 题目下载到了根目录而不是 Current？
1. 运行任务 **`Codeforces: Move Files to Current`** 将散落的文件整理进去。
2. 确保 `.vscode/settings.json` 中配置了 `"cph.general.saveLocation": "${workspaceFolder}/Current"`。

---

## ⌨️ 快捷键建议

建议在 `keybindings.json` 中绑定以下快捷键以提高效率：

```json
[
    {
        "key": "cmd+shift+s",
        "command": "workbench.action.tasks.runTask",
        "args": "Codeforces: Sync"
    }
]
```
